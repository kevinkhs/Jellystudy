spring:
  application:
    name: jellystudy-knowledge-point-provider
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_CONFIG_SERVER_ADDR:nacos:8848}
        file-extension: yaml
        group: DEFAULT_GROUP
      discovery:
        server-addr: ${NACOS_CONFIG_SERVER_ADDR:nacos:8848}
